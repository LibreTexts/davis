---
title: Input
description: Accessible text input component with validation states and helper text
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import PropsTable from "../../../components/PropsTable.astro";
import Example from "../../../components/Example.astro";
import InputBasicReact from "../../../examples/react/InputBasic.tsx";
import InputBasicReactSrc from "../../../examples/react/InputBasic.tsx?raw";
import InputBasicVue from "../../../examples/vue/InputBasic.vue";
import InputBasicVueSrc from "../../../examples/vue/InputBasic.vue?raw";

Text inputs allow users to enter and edit text. The Davis Input component includes labels, validation states, helper text, and icon support.

## Installation

<Tabs syncKey="framework">
<TabItem label="React">
```bash
npm install @libretexts/davis-react
```
</TabItem>
<TabItem label="Vue">
```bash
npm install @libretexts/davis-vue
```

</TabItem>
</Tabs>

## Basic Usage

<Tabs syncKey="framework">
  <TabItem label="React">
    <Example code={InputBasicReactSrc} lang="tsx">
      <InputBasicReact client:load />
    </Example>
  </TabItem>
  <TabItem label="Vue">
    <Example code={InputBasicVueSrc} lang="vue">
      <InputBasicVue client:load />
    </Example>
  </TabItem>
</Tabs>

## Sizes

Inputs are available in three sizes.

<Tabs syncKey="framework">
<TabItem label="React">
```tsx
<Input name="small" label="Small Input" size="sm" />
<Input name="medium" label="Medium Input" size="md" />
<Input name="large" label="Large Input" size="lg" />
```
</TabItem>
<TabItem label="Vue">
```vue
<Input name="small" label="Small Input" size="sm" />
<Input name="medium" label="Medium Input" size="md" />
<Input name="large" label="Large Input" size="lg" />
```

</TabItem>
</Tabs>

## Required Fields

Mark inputs as required with the `required` prop, which adds an asterisk to the label.

<Tabs syncKey="framework">
<TabItem label="React">
```tsx
<Input
  name="username"
  label="Username"
  required
/>
```
</TabItem>
<TabItem label="Vue">
```vue
<Input
  name="username"
  label="Username"
  required
/>
```

</TabItem>
</Tabs>

## Helper Text

Provide additional context with helper text.

<Tabs syncKey="framework">
<TabItem label="React">
```tsx
<Input
  name="password"
  label="Password"
  type="password"
  helperText="Must be at least 8 characters"
/>
```
</TabItem>
<TabItem label="Vue">
```vue
<Input
  name="password"
  label="Password"
  type="password"
  helper-text="Must be at least 8 characters"
/>
```

</TabItem>
</Tabs>

## Error State

Show validation errors with the `error` and `errorMessage` props.

<Tabs syncKey="framework">
<TabItem label="React">
```tsx
import { Input } from '@libretexts/davis-react';
import { useState } from 'react';

export default function Example() {
  const [email, setEmail] = useState('');
  const isInvalid = email && !email.includes('@');

  return (
    <Input
      name="email"
      label="Email"
      type="email"
      value={email}
      onChange={(e) => setEmail(e.target.value)}
      error={isInvalid}
      errorMessage={isInvalid ? 'Please enter a valid email address' : undefined}
    />
  );
}
```
</TabItem>
<TabItem label="Vue">
```vue
<script setup>
import { Input } from '@libretexts/davis-vue';
import { ref, computed } from 'vue';

const email = ref('');
const isInvalid = computed(() => email.value && !email.value.includes('@'));
</script>

<template>
  <Input
    name="email"
    label="Email"
    type="email"
    v-model="email"
    :error="isInvalid"
    :error-message="isInvalid ? 'Please enter a valid email address' : undefined"
  />
</template>
```

</TabItem>
</Tabs>

## With Icons

Add left or right icons to enhance the input's purpose.

<Tabs syncKey="framework">
<TabItem label="React">
```tsx
import { Input } from '@libretexts/davis-react';

function SearchIcon() {
  return (
    <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
    </svg>
  );
}

export default function Example() {
  return (
    <Input
      name="search"
      label="Search"
      leftIcon={<SearchIcon />}
      placeholder="Search..."
    />
  );
}
```
</TabItem>
<TabItem label="Vue">
```vue
<script setup>
import { Input } from '@libretexts/davis-vue';
</script>

<template>
  <Input
    name="search"
    label="Search"
    placeholder="Search..."
  >
    <template #left-icon>
      <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
    </template>
  </Input>
</template>
```

</TabItem>
</Tabs>

## Props

<Tabs syncKey="framework">
  <TabItem label="React">
    <PropsTable component="input" framework="react" />
  </TabItem>
  <TabItem label="Vue">
    <PropsTable component="input" framework="vue" />
  </TabItem>
</Tabs>

## Accessibility

The Input component is built with accessibility in mind:

- Associates labels with inputs using `htmlFor` and `id`
- Uses `aria-invalid` for error states
- Uses `aria-describedby` for helper text and error messages
- Supports `required` attribute for form validation
- Visual required indicator (asterisk) for screen reader users
- Proper color contrast for all states

**Best practices:**
- Always provide a descriptive label
- Use `helperText` to provide input formatting guidance
- Use `errorMessage` for validation feedback
- Use `required` prop for required fields
- Ensure custom icons have appropriate ARIA labels if they convey meaning
